apiVersion: v1
kind: Service
metadata:
  namespace: customApp
  labels:
    app: custom-internal
  name: custom-internal
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: custom-internal
  type: LoadBalancer
status:
  loadBalancer: {}
